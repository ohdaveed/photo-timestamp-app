---
name: update-features-list
description: Workflow command scaffold for update-features-list in photo-timestamp-app.
allowed_tools: ["Bash", "Read", "Write", "Grep", "Glob"]
---

# /update-features-list

Use this workflow when working on **update-features-list** in `photo-timestamp-app`.

## Goal

Adds or updates feature suggestions or proposals in the FEATURES.md file.

## Common Files

- `FEATURES.md`

## Suggested Sequence

1. Understand the current state and failure mode before editing.
2. Make the smallest coherent change that satisfies the workflow goal.
3. Run the most relevant verification for touched files.
4. Summarize what changed and what still needs review.

## Typical Commit Signals

- Edit FEATURES.md to add or update feature proposals.
- Commit the changes.
- Optionally, merge via pull request.

## Notes

- Treat this as a scaffold, not a hard-coded script.
- Update the command if the workflow evolves materially.